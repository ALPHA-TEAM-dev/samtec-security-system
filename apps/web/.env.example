# Optional. Copy this file to `.env.local` in this folder to change settings.
# Everything in these files ends up in the browser, so never put secrets here.

# Where the SAMTEC API runs. Used by `pnpm dev:web:live` and production builds.
VITE_API_BASE_URL=http://localhost:3000/api/v1
